locals {
  # Configure standard naming convention for relevant resources
  vng_name = "vng"
  pip_name= "pip"

  # Virtual Network Gateway configuration
  vng_type = "Vpn"
  vpn_type = "RouteBased"
}
